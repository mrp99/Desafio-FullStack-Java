export interface Pais {
  id: string,
  nome: string,
  sigla: string,
  continente: string,
  ddi: number
}

//ddi = codigo Ddi
export interface NovoPais {
  nome: string,
  sigla: string,
  continente: string,
  ddi: number
}
